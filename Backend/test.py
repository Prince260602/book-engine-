1.clean py files - it will expect the argument and return the op --- simple - only api-ip op

2. trigger py file which will trigger all the other files taking ip manually/hardcoded-to test files working proprly-hard coded values

3.read fast api docs & create the shell functions/api's for each function

4.error handling

get--no body
post--pass body